---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificate-manager-controller-dev
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: kubernetes/flux/apps/dev/certificate-manager/controller
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: certificate-manager-configuration-dev
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: kubernetes/flux/apps/dev/certificate-manager/configuration
  dependsOn:
    - name: certificate-manager-controller
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg