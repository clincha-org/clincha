---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-dev
  namespace: flux-system
spec:
  interval: 1m
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/flux/apps/dev/traefik
  dependsOn:
    - name: certificate-manager-configuration-dev
    - name: metallb-configuration-dev
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg