---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: monitoring
  namespace: flux-system
spec:
  interval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/flux/infrastructure/dev/monitoring
  dependsOn:
    - name: certificate-manager-configuration
    - name: metallb-configuration
    - name: traefik
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg