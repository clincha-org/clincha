---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nebula-sync
  namespace: flux-system
spec:
  interval: 1m
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/flux/applications/hawkfield/nebula-sync
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
