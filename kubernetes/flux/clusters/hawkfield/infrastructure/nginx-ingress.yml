---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ingress-nginx
  namespace: flux-system
spec:
  interval: 1m
  timeout: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/flux/infrastructure/hawkfield/ingress-nginx
  dependsOn:
    - name: metallb-configuration
  prune: true
  wait: true
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg