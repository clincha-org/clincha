---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: uptime-kuma-certificate
  namespace: uptime-kuma
spec:
  secretName: uptime-kuma-certificate-secret
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - status.clinch-home.com