apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: monitoring
resources:
  - ../../base/monitoring
configMapGenerator:
  - name: prometheus.yml
    namespace: monitoring
    files:
      - prometheus.yml
    options:
      disableNameSuffixHash: true