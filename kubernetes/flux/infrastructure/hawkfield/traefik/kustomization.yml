apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: traefik
resources:
  - ../../base/traefik
configMapGenerator:
  - name: traefik-values
    namespace: traefik
    files:
      - values.yml
    options:
      disableNameSuffixHash: true