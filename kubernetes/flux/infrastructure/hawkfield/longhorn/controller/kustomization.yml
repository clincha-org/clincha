apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: longhorn-controller
resources:
  - ../../../base/longhorn/controller
configMapGenerator:
  - namespace: longhorn
    name: longhorn-values
    files:
      - values.yml
    options:
      disableNameSuffixHash: true