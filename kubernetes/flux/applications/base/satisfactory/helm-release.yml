---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: satisfactory
  namespace: satisfactory
spec:
  targetNamespace: satisfactory
  interval: 1m
  chart:
    spec:
      chart: satisfactory
      version: '1.1.1'
      sourceRef:
        kind: HelmRepository
        name: naj98
        namespace: satisfactory
      interval: 1m
  valuesFrom:
    - kind: ConfigMap
      name: satisfactory-values
      valuesKey: values.yml
