---
apiVersion: v1
kind: Service
metadata:
  name: giftlist
  namespace: giftlist
spec:
  selector:
    name: giftlist
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
