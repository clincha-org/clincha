apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: logging
resources:
  - ../../base/elastic-logging
configMapGenerator:
  - name: elastic-values
    namespace: logging
    files:
      - values.yml
    options:
      disableNameSuffixHash: true