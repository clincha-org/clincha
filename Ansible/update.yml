- name: "Update packages"
  become: true
  tasks:
    - name: "Update packages"
      ansible.builtin.package:
        name: "*"
        state: latest

#    - name: "Restart host"
#      ansible.builtin.reboot: