---
- name: "Pi-hole configuration"
  block:
    - name: "Install"
      ansible.builtin.include_tasks: install.yml

    - name: "Configure"
      ansible.builtin.include_tasks: configure.yml

    - name: "DNS records"
      ansible.builtin.include_tasks: dns-records.yml
